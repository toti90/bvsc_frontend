import { NumberHourConverterPipe } from './number-hour-converter.pipe';

describe('NumberHourConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberHourConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
