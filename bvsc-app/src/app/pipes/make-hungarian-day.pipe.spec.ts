import { MakeHungarianDayPipe } from './make-hungarian-day.pipe';

describe('MakeHungarianDayPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeHungarianDayPipe();
    expect(pipe).toBeTruthy();
  });
});
